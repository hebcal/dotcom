<VirtualHost *:8080>
	ServerName download.hebcal.com
	ServerAlias localhost

	ServerAdmin webmaster@hebcal.com
	DocumentRoot /var/www/download.hebcal.com

        <Directory />
                Options FollowSymLinks
                AllowOverride None
        </Directory>
        <Directory /var/www/download.hebcal.com/>
                AllowOverride All
                Options +ExecCGI
                AddHandler cgi-script .cgi
                Order allow,deny
                allow from all
        </Directory>

	LogFormat "%a %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-agent}i\"" combined2
	ErrorLog ${APACHE_LOG_DIR}/error.log
	CustomLog ${APACHE_LOG_DIR}/access.log combined2

	<IfModule mod_deflate.c>
	 AddOutputFilterByType DEFLATE text/html text/css text/javascript
	</IfModule>

        <IfModule mod_remoteip.c>
         RemoteIPHeader X-Forwarded-For
         RemoteIPInternalProxy 127.0.0.1
        </IfModule>
</VirtualHost>

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
